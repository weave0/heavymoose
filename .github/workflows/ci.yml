name: CI

on:
    pull_request:
        branches: [ main ]
    push:
        branches: [ main ]

jobs:
    validate:
        runs-on: ubuntu-latest
        name: Validate HTML

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: "20"

            - name: Install dependencies
              run: npm ci

            - name: Lint HTML
              run: npm run lint:html

            - name: Check cache-bust is current
              run: node scripts/update-cache-bust.js
